-- Framework Settings
notify = 'qb' 
--  Support List
--  'qb' QBcore
--  'okok' okokNotify

-- bacs-nametag system
Enable = true
d_Distance = 15   -- Display distance

-- Beginner's system
-- Enable -- Enable the beginner system | Enable true | Disable false
-- time -- beginner's time | Default 1 day | specified in seconds
beginner = {
    Enable = true,  
    newbie_text = '🔰',
    time = 60 * 60 * 24
}

-- Stream system
stream = {
    Enable = true,
    stream_icon = '🛰'
}